上海的天气 -> llm -> 知道要调用skill接口 -> 返回的chunk 里面带有 tools -> 先自己创建个stream1解析，要是普通数据直接encode,如果带有tool_calls的则收集起来

——> 加上‘TOOL_CALLS‘特殊标识encode给route -> route用双层循环，外层接受流,直层判断流，普通数据encode，TOOL_CALLS出现，外层的flag为true 进入外层if判断从而 调用工具，使用await 等待返回，然后获取到接口返回值，进入外层循环第二次执行， -> llm ->直到结束
